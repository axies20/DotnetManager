# SDK sources managed by DotnetManager.
#
# Track the newest SDK accepted by a release policy:
# channel <version|LTS|STS> <ga|go-live|preview>
#
# Pin a particular published SDK:
# version <sdk-version>

channel 10.0 ga
